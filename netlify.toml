[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev"
  NODE_ENV = "development"

[functions]
  directory = "dist/server"
  node_bundler = "none"
